/** @type {import('next').NextConfig} */
const nextConfig = {
	i18n: {
		locales: ["fr", "ar"], // Add the languages you want to support here
		defaultLocale: "fr", // Set the default locale
	},
};

module.exports = nextConfig;
